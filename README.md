### Installation of the miniconda enviroment 
This entire project  is containerized via Conda for simplifying dependency management and reproducibility. 

To replicate this exact environment, clone the repository, navigate to the folder, and run:
```bash
conda env create -f environment.yml
conda activate condalearn
